[package]
name = "backend_vulkan"
version = "0.1.0"
edition = "2021"

[dependencies]
gfx = {path = "../../common/gfx"}
maths = {path = "../../maths"}
macros = {path = "../../macros"}
ash = "0.37.0"
gpu-allocator =  { version= "0.19.0", default-features = false, features=["vulkan"]}