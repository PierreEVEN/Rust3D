[package]
name = "core"
version = "0.1.0"
edition = "2021"

[dependencies]
shader_compiler = { path = "../shader_compiler" }
gfx = { path = "../common/gfx" }
plateform = { path = "../common/plateform" }
gfx-maths = "0.2.8"
logger = {path = "../logger"}
lazy_static = "1.4.0"